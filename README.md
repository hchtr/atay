# atay

"atay" is a C project focused on building a clean, performant rendering layer from scratch using [SDL](https://www.libsdl.org/).

## Getting Started

### Cloning the Project
This project uses Git submodules for dependencies. To clone the repository and fetch SDL, use:

```bash
git clone --recursive git@github.com:hchtr/atay.git
